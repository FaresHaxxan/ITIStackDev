// Show a message about the first release
alert("Welcome to the first release of the calculator! This version only supports summation.");

// Ask the user to enter the first number
var firstNumber = parseFloat(prompt("Enter the first number:"));

// Ask the user to enter the second number
var secondNumber = parseFloat(prompt("Enter the second number:"));

// Calculate the summation result
var summationResult = firstNumber + secondNumber;

// Show the summation result
alert (firstNumber + " + " + secondNumber + " = " + summationResult);